Starten & Testen mit mvn Wrapper:
.\mvnw.cmd clean verify exec:exec

Starten mit mvn Wrapper:
.\mvnw.cmd clean compile exec:exec

Testen mit mvn Wrapper:
.\mvnw.cmd clean install


jar starten
java -jar target/stateofstructure-1.0-SNAPSHOT.jar

jar bauen
 .\mvnw.cmd clean package          